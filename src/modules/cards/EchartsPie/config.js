
import React from "react";
// import BaseSearch from "./component/option";

const name = 'line';
const cardModule = 'cards';
const cardType = 'EchartsPie';

export default {
    name,
    cardModule,
    cardType,
    // isSingleFormPage: true,
    hideHeader: false,
    // renderCustom: props => <BaseSearch {...props} />,
    filters: {
        visible: false
    }
}